<Window x:Class="Astrarium.Plugins.Horizon.Views.LandscapeEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:a="Astrarium"
        mc:Ignorable="d"
        Title="{a:Text LandscapeEditorView.Title}" 
        Style="{StaticResource {x:Type Window}}" 
        MinHeight="400" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        a:WindowProperties.MinButtonVisible="Collapsed"
        a:WindowProperties.MaxButtonVisible="Collapsed"
        Height="400" Width="800">
    <Grid Margin="8">
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Content="{a:Text LandscapeEditorView.Landscape.Title}" Grid.Column="0" />
                <TextBox Text="{Binding Title}" Grid.Column="1" />

                <Label Content="{a:Text LandscapeEditorView.Landscape.AzimuthShift}" Grid.Column="0" Grid.Row="1"  />

                <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1">
                    <a:NumericUpDown Width="80" Minimum="-360" Maximum="360" Step="1" DecimalPlaces="2" Value="{Binding AzimuthShift}"   />
                    <TextBlock Margin="8 0" Text="{a:Text LandscapeEditorView.Landscape.AzimuthShift.Comments}" IsEnabled="False" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>

            <Label Grid.Row="1" Content="{a:Text LandscapeEditorView.Landscape.Description}" />
            <TextBox VerticalScrollBarVisibility="Auto" Grid.Row="2" Text="{Binding Description}" TextWrapping="Wrap" Height="Auto" AcceptsReturn="True" />

            <StackPanel Margin=" 0 8 0 0" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button MinWidth="80" Margin="4 0 0 0" Content="{a:Text OK}" Command="{Binding OKCommand}" />
                <Button MinWidth="80" Margin="4 0 0 0" Content="{a:Text Cancel}" Command="{Binding CancelCommand}" />
            </StackPanel>

        </Grid>

        
    </Grid>
</Window>
