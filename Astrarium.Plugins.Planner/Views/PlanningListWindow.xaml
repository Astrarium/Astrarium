<Window x:Class="Astrarium.Plugins.Planner.Views.PlanningListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:a="Astrarium"
        xmlns:v="clr-namespace:Astrarium.Plugins.Planner.Views" 
        xmlns:c="clr-namespace:Astrarium.Plugins.Planner.Controls"
        Title="{a:Text PlanningListWindow.Title}"
        Style="{StaticResource {x:Type Window}}"
        WindowStartupLocation="CenterScreen"
        MinHeight="300" MinWidth="800"
        Height="500" Width="800">

    <Window.Resources>
        <ContextMenu x:Key="RowMenu" DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.ShowObject}" Command="{Binding ShowObjectCommand}" CommandParameter="{Binding SelectedTableItem.CelestialObject}" IsEnabled="{Binding IsSigleTableItemSelected}" />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.SlewTelescope}" Command="{Binding SlewTelescopeCommand}" CommandParameter="{Binding SelectedTableItem.CelestialObject}" Visibility="{Binding IsTelescopeAvailable, Converter={a:BoolToVisibilityConverter}}" IsEnabled="{Binding IsTelescopeConnected}" />
            <Separator />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.SetBeginTime}" Command="{Binding SetTimeCommand}" CommandParameter="{Binding SelectedTableItem[Observation.Begin]}" IsEnabled="{Binding IsGoToObservationBeginEnabled}" />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.SetBestTime}" Command="{Binding SetTimeCommand}" CommandParameter="{Binding SelectedTableItem[Observation.Best]}"  IsEnabled="{Binding IsGoToObservationBestEnabled}" />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.SetEndTime}" Command="{Binding SetTimeCommand}" CommandParameter="{Binding SelectedTableItem[Observation.End]}"  IsEnabled="{Binding IsGoToObservationEndEnabled}" />
            <Separator />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.AddObjectByName}" Command="{Binding AddObjectCommand}" InputGestureText="Ctrl+F" />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.AddObjectsByFilter}" Command="{Binding AddObjectsCommand}" InputGestureText="Ctrl+I" />
            <Separator />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.RemoveSelected}" Command="{Binding RemoveSelectedItemsCommand}" IsEnabled="{Binding SelectedTableItem, Converter={a:NotNullToBoolConverter}}" InputGestureText="Delete" />
            <Separator />
            <MenuItem Header="{a:Text PlanningListWindow.ContextMenu.Save}" Command="{Binding SaveCommand}" />

        </ContextMenu>
        <Style x:Key="DefaultRowStyle" TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource {x:Type DataGridRow}}">
            <Setter Property="ContextMenu" Value="{StaticResource RowMenu}" />
        </Style>
        <Style x:Key="DateLinkStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Height" Value="10" />
            <Setter Property="FontFamily" Value="Lucida Console" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <DrawingImage x:Key="ImageUnsaved">
            <DrawingImage.Drawing>
                <DrawingGroup ClipGeometry="M0,0 V125 H100 V0 H0 Z">
                    <GeometryDrawing Brush="{StaticResource BrushControlLightBackground}" Geometry="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z" />
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>

        <Grid x:Key="ImageFilter" x:Shared="False">
            <Path HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="True" Width="20" Height="20" Margin="0 4 0 0" Stretch="Uniform" Fill="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}, Path=Foreground}">
                <Path.Data>
                    <StreamGeometry>F1 M700,700z M0,0z M140,70L303.34,256.66 303.34,443.32 396.676,489.984 396.676,256.644 560.016,69.9839999999999 350.016,69.9839999999999z</StreamGeometry>
                </Path.Data>
                <Path.LayoutTransform>
                    <RotateTransform Angle="0"></RotateTransform>
                </Path.LayoutTransform>
            </Path>
        </Grid>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="F" Command="{Binding AddObjectCommand}" />
        <KeyBinding Modifiers="Control" Key="I" Command="{Binding AddObjectsCommand}" />
    </Window.InputBindings>

    <Grid Margin="0 4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Margin="8 2 18 0" Orientation="Vertical">
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock FontSize="30" Text="{Binding Date, Converter={a:DateToStringConverter}, Mode=OneWay}" IsEnabled="False" Grid.ColumnSpan="2" />

                    <TextBlock Grid.Column="0" Grid.Row="1" Margin="0 6 10 0" Text="{a:Text PlanningListWindow.From}" IsEnabled="False"  />
                    <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding FromTime, Converter={a:TimeSpanToStringConverter}}" Margin="0 6 0 0" HorizontalAlignment="Right" />
                    <TextBlock Grid.Column="0" Margin="0 0 10 0" Grid.Row="2" Text="{a:Text PlanningListWindow.To}" IsEnabled="False"  />
                    <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding ToTime, Converter={a:TimeSpanToStringConverter}}" HorizontalAlignment="Right" />
                    <TextBlock Grid.Column="0" Margin="0 0 10 0"  Grid.Row="3" Text="{a:Text PlanningListWindow.Duration}" IsEnabled="False"  />
                    <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding DurationString, Mode=OneWay}" HorizontalAlignment="Right" />
                    <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="4" Margin="0 8 10 0" Text="{a:Text PlanningListWindow.ObserverLocation}" IsEnabled="False"  />
                    <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="5" Text="{Binding GeoLocation.LocationName, Mode=OneWay}" IsEnabled="False" />
                </Grid>
            </StackPanel>

            <c:DirectionChart 
                Height="140"
                Width="140"
                Margin="8"
                SnapsToDevicePixels="True"
                ShowChart="{Binding IsSigleTableItemSelected, Mode=OneWay}"
                FromTime="{Binding FromTime, Mode=OneWay}"
                ToTime="{Binding ToTime, Mode=OneWay}"
                SunCoordinates="{Binding SunCoordinates}" 
                BodyCoordinates="{Binding BodyCoordinates}"
                SiderealTime="{Binding SiderealTime}"
                GeoLocation="{Binding GeoLocation}"
                Grid.Column="1" 
                HorizontalAlignment="Stretch" 
                IsDarkMode="{Binding IsDarkMode, Mode=OneWay}" />

            <StackPanel x:Name="Container" Grid.Column="2" HorizontalAlignment="Stretch">
                <c:VisibilityChart 
                    Grid.Column="2"
                    Width="{Binding ElementName=Container, Path=ActualWidth}"
                    Height="140"                    
                    Margin="8"
                    SnapsToDevicePixels="True"
                    ShowChart="{Binding IsSigleTableItemSelected, Mode=OneWay}"
                    FromTime="{Binding FromTime, Mode=OneWay}"
                    ToTime="{Binding ToTime, Mode=OneWay}"
                    SunCoordinates="{Binding SunCoordinates}" 
                    BodyCoordinates="{Binding BodyCoordinates}"
                    SiderealTime="{Binding SiderealTime}"
                    GeoLocation="{Binding GeoLocation}"
                    IsDarkMode="{Binding IsDarkMode, Mode=OneWay}"
                    HorizontalAlignment="Stretch" />
            </StackPanel>

            <Grid Grid.Column="3" Width="20" />

        </Grid>

        <Grid Margin="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="4 0" Padding="8 0" Content="{a:Text PlanningListWindow.AddObjectByName.Button}" ToolTip="{a:Text PlanningListWindow.AddObjectByName.Button.Tooltip}" Command="{Binding AddObjectCommand}" />
            <Button Grid.Column="1" Margin="2 0" Padding="8 0" Content="{a:Text PlanningListWindow.AddObjectsByFilter.Button}"  ToolTip="{a:Text PlanningListWindow.AddObjectsByFilter.Button.Tooltip}" Command="{Binding AddObjectsCommand}" />
            <Button Grid.Column="2" Margin="4 0" Padding="8 0" Content="{a:Text PlanningListWindow.Save.Button}" Command="{Binding SaveCommand}" />
            <a:SearchTextBox Grid.Column="3" Margin="2 0 2 0" Watermark="{a:Text PlanningListWindow.Filter.Hint}" HorizontalAlignment="Stretch" Text="{Binding FilterString, Mode=TwoWay, Delay=1000, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <TextBlock TextAlignment="Center" Padding="20" TextWrapping="Wrap" Visibility="{Binding NoTotalItems, Converter={a:BoolToVisibilityConverter}}" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Run IsEnabled="False" Text="{a:Text PlanningListWindow.NoObjects.YouCan}"></Run>
            <Hyperlink Command="{Binding AddObjectCommand}"><Run Text="{a:Text PlanningListWindow.NoObjects.AddObjectByName}" /></Hyperlink>
            <Run IsEnabled="False" Text="{a:Text PlanningListWindow.NoObjects.Or}" />
            <Hyperlink Command="{Binding AddObjectsCommand}"><Run Text="{a:Text PlanningListWindow.NoObjects.AddObjectsByFilter}" /></Hyperlink>
        </TextBlock>
        <TextBlock TextAlignment="Center" Padding="20" TextWrapping="Wrap" Visibility="{Binding NoFilteredItems, Converter={a:BoolToVisibilityConverter}}" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" >            
            <Run IsEnabled="False" Text="{a:Text PlanningListWindow.NoFilteredObjects}" />
            <LineBreak></LineBreak>
            <Hyperlink Command="{Binding ClearFilterCommand}"><Run Text="{a:Text PlanningListWindow.ClearFilter}" /></Hyperlink>        
        </TextBlock>

        <c:ExtendedDataGrid 
            Visibility="{Binding HasItemsToDisplay, Converter={a:BoolToVisibilityConverter}}"
            Style="{StaticResource {x:Type DataGrid}}"
            Grid.Row="2"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
            EnableRowVirtualization="True"
            EnableColumnVirtualization="True"
            VirtualizingPanel.IsVirtualizingWhenGrouping="True"
            VirtualizingPanel.VirtualizationMode="Recycling"
            VirtualizingPanel.IsVirtualizing="True"
            AutoGenerateColumns="False"
            CanUserReorderColumns="True"
            CanUserSortColumns="True"
            CanUserResizeRows="False"
            ScrollViewer.IsDeferredScrollingEnabled="True"
            IsReadOnly="True"
            GridLinesVisibility="Vertical"
            BorderThickness="1"
            Margin="4"
            RowHeaderWidth="0"
            ColumnHeaderHeight="32"
            RowHeight="22"
            SelectionMode="Extended"
            SelectionUnit="FullRow"
            RowStyle="{StaticResource DefaultRowStyle}"
            ItemsSource="{Binding TableData, IsAsync=True}"
            IsSynchronizedWithCurrentItem="True"
            SelectedItem="{Binding SelectedTableItem}"
            SelectedItems="{Binding SelectedTableItems, Mode=OneWayToSource}">

            <DataGrid.InputBindings>
                <KeyBinding Key="Delete" Command="{Binding RemoveSelectedItemsCommand}" />
            </DataGrid.InputBindings>

            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Expander Header="{Binding Name}">
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>
            
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.Object}" SortMemberPath="CelestialObject.Names[0]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Border Background="Transparent" ToolTip="{Binding CelestialObject, Converter={a:CelestialObjectTypeDescriptionConverter}}">
                                    <a:PathIcon x:Name="Icon" Margin="2" Width="16" Height="16" IsEnabled="False" Data="{Binding CelestialObject, Converter={a:CelestialObjectToIconConverter}}" />
                                </Border>
                                <TextBlock Padding="4 0 0 0" Text="{Binding CelestialObject, Converter={a:CelestialObjectNameConverter}}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="{a:Text PlanningListWindow.Table.Mag}" Binding="{Binding Path='[Magnitude]', Converter={v:Converter}, ConverterParameter='Magnitude'}" SortMemberPath="[Magnitude]" />

                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.BeginObs}" CanUserSort="True" SortMemberPath="[Observation.Begin]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Visibility="{Binding Path='[Observation.Begin]', Converter={v:TimeLinkVisibilityConverter}}" Style="{StaticResource DateLinkStyle}">
                                    <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type v:PlanningListWindow}, Mode=FindAncestor}, Path=DataContext.SetTimeCommand}" CommandParameter="{Binding Path='[Observation.Begin]'}"><Run Text="{Binding Path='[Observation.Begin]', Mode=OneWay,  Converter={v:Converter}, ConverterParameter='Visibility.Begin'}" /></Hyperlink>
                                </TextBlock>
                                <TextBlock IsEnabled="False" Visibility="{Binding Path='[Observation.Begin]', Converter={v:TimeLinkInverseVisibilityConverter}}" Text="—" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.BestTime}" CanUserSort="True" SortMemberPath="[Observation.Best]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Visibility="{Binding Path='[Observation.Best]', Converter={v:TimeLinkVisibilityConverter}}" Style="{StaticResource DateLinkStyle}">
                                    <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type v:PlanningListWindow}, Mode=FindAncestor}, Path=DataContext.SetTimeCommand}" CommandParameter="{Binding Path='[Observation.Best]'}"><Run Text="{Binding Path='[Observation.Best]', Mode=OneWay,  Converter={v:Converter}, ConverterParameter='Visibility.Begin'}" /></Hyperlink>
                                </TextBlock>
                                <TextBlock IsEnabled="False" Visibility="{Binding Path='[Observation.Best]', Converter={v:TimeLinkInverseVisibilityConverter}}" Text="—" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.EndObs}" CanUserSort="True" SortMemberPath="[Observation.End]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Visibility="{Binding Path='[Observation.End]', Converter={v:TimeLinkVisibilityConverter}}" Style="{StaticResource DateLinkStyle}">
                                    <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type v:PlanningListWindow}, Mode=FindAncestor}, Path=DataContext.SetTimeCommand}" CommandParameter="{Binding Path='[Observation.End]'}"><Run Text="{Binding Path='[Observation.End]', Mode=OneWay,  Converter={v:Converter}, ConverterParameter='Visibility.End'}" /></Hyperlink>
                                </TextBlock>
                                <TextBlock IsEnabled="False" Visibility="{Binding Path='[Observation.End]', Converter={v:TimeLinkInverseVisibilityConverter}}" Text="—" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="{a:Text PlanningListWindow.Table.ObsDur}" Binding="{Binding Path='[Observation.Duration]', Converter={v:Converter}, ConverterParameter='Visibility.Duration'}" SortMemberPath="[Observation.Duration]" />

                <DataGridTextColumn Binding="{Binding Path='[Observation.BestAltitude]', Converter={v:Converter}, ConverterParameter='RTS.TransitAltitude'}" Header="{a:Text PlanningListWindow.Table.BestAlt}" CanUserSort="True" SortMemberPath="[Observation.BestAltitude]" />
                <DataGridTextColumn Binding="{Binding Path='[Observation.BestAzimuth]', Converter={v:Converter}, ConverterParameter='RTS.RiseAzimuth'}" Header="{a:Text PlanningListWindow.Table.BestAzi}" CanUserSort="True" SortMemberPath="[Observation.BestAzimuth]" />

                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.Rise}" CanUserSort="True" SortMemberPath="[RTS.Rise]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Visibility="{Binding Path='[RTS.Rise]', Converter={v:TimeLinkVisibilityConverter}}" Style="{StaticResource DateLinkStyle}">
                                    <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type v:PlanningListWindow}, Mode=FindAncestor}, Path=DataContext.SetTimeCommand}" CommandParameter="{Binding Path='[RTS.Rise]'}"><Run Text="{Binding Path='[RTS.Rise]', Mode=OneWay, Converter={v:Converter}, ConverterParameter='RTS.Rise'}" /></Hyperlink>
                                </TextBlock>
                                <TextBlock IsEnabled="False" Visibility="{Binding Path='[RTS.Rise]', Converter={v:TimeLinkInverseVisibilityConverter}}" Text="—" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.Transit}" CanUserSort="True" SortMemberPath="[RTS.Transit]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Visibility="{Binding Path='[RTS.Transit]', Converter={v:TimeLinkVisibilityConverter}}" Style="{StaticResource DateLinkStyle}">
                                    <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type v:PlanningListWindow}, Mode=FindAncestor}, Path=DataContext.SetTimeCommand}" CommandParameter="{Binding Path='[RTS.Transit]'}"><Run Text="{Binding Path='[RTS.Transit]', Mode=OneWay, Converter={v:Converter}, ConverterParameter='RTS.Transit'}" /></Hyperlink>
                                </TextBlock>
                                <TextBlock IsEnabled="False" Visibility="{Binding Path='[RTS.Transit]', Converter={v:TimeLinkInverseVisibilityConverter}}" Text="—" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="{a:Text PlanningListWindow.Table.Set}" CanUserSort="True" SortMemberPath="[RTS.Set]">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Visibility="{Binding Path='[RTS.Set]', Converter={v:TimeLinkVisibilityConverter}}" Style="{StaticResource DateLinkStyle}">
                                    <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type v:PlanningListWindow}, Mode=FindAncestor}, Path=DataContext.SetTimeCommand}" CommandParameter="{Binding Path='[RTS.Set]'}"><Run Text="{Binding Path='[RTS.Set]', Mode=OneWay, Converter={v:Converter}, ConverterParameter='RTS.Set'}" /></Hyperlink>
                                </TextBlock>
                                <TextBlock IsEnabled="False" Visibility="{Binding Path='[RTS.Set]', Converter={v:TimeLinkInverseVisibilityConverter}}" Text="—" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Binding="{Binding Path='[RTS.RiseAzimuth]', Converter={v:Converter}, ConverterParameter='RTS.RiseAzimuth'}" Header="{a:Text PlanningListWindow.Table.RiseAzimuth}" CanUserSort="True" SortMemberPath="[RTS.RiseAzimuth]" />
                <DataGridTextColumn Binding="{Binding Path='[RTS.TransitAltitude]', Converter={v:Converter}, ConverterParameter='RTS.TransitAltitude'}" Header="{a:Text PlanningListWindow.Table.TransitAltitude}" CanUserSort="True" SortMemberPath="[RTS.TransitAltitude]" />
                <DataGridTextColumn Binding="{Binding Path='[RTS.SetAzimuth]', Converter={v:Converter}, ConverterParameter='RTS.SetAzimuth'}" Header="{a:Text PlanningListWindow.Table.SetAzimuth}" CanUserSort="True" SortMemberPath="[RTS.SetAzimuth]" />
                <DataGridTextColumn Binding="{Binding Path='[Equatorial.Alpha]', Converter={v:Converter}, ConverterParameter='Equatorial.Alpha'}" Header="{a:Text PlanningListWindow.Table.RA}" CanUserSort="True" SortMemberPath="[Equatorial.Alpha]" />
                <DataGridTextColumn Binding="{Binding Path='[Equatorial.Delta]', Converter={v:Converter}, ConverterParameter='Equatorial.Delta'}" Header="{a:Text PlanningListWindow.Table.Dec}" CanUserSort="True" SortMemberPath="[Equatorial.Delta]" />
                <DataGridTextColumn Binding="{Binding Path='[Constellation]', Converter={v:Converter}, ConverterParameter='Constellation'}" Header="{a:Text PlanningListWindow.Table.Constellation}" CanUserSort="True" SortMemberPath="[Constellation]" />

            </DataGrid.Columns>
            
        </c:ExtendedDataGrid>

        <StatusBar Height="24" Grid.Row="3" Margin="4 0" BorderThickness="1">
            <StatusBarItem>
                <StackPanel IsEnabled="False" Orientation="Horizontal">
                    <TextBlock Text="{a:Text PlanningListWindow.StatusBar.TotalObjects}" />
                    <TextBlock Text="{Binding TotalItemsCount, FallbackValue=0}" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel IsEnabled="False" Orientation="Horizontal">
                    <TextBlock Text="{a:Text PlanningListWindow.StatusBar.Filtered}" />
                    <TextBlock Text="{Binding FilteredItemsCount, FallbackValue=0}" />
                </StackPanel>
            </StatusBarItem>

            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Visibility="{Binding FilePath, Converter={a:NotNullToVisibilityConverter}}">
                        <Image Source="{StaticResource ImageUnsaved}" Margin="0 2 4 0" Stretch="UniformToFill" Width="10" Height="10" Visibility="{Binding IsSaved, Converter={a:InverseBoolToVisibilityConverter}}" ToolTip="{a:Text PlanningListWindow.StatusBar.UnsavedChanges}" />
                        <TextBlock IsEnabled="False" Text="{Binding FilePath}" />
                    </StackPanel>
                    <TextBlock IsEnabled="False" Text="{a:Text PlanningListWindow.StatusBar.PlanNotSaved}" Visibility="{Binding FilePath, Converter={a:NullToVisibilityConverter}}"  />
                </StackPanel>
            </StatusBarItem>

        </StatusBar>

    </Grid>
</Window>
