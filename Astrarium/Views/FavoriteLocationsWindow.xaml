<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:c="clr-namespace:Astrarium.Types.Controls;assembly=Astrarium.Types" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:a="Astrarium" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    x:Class="Astrarium.Views.FavoriteLocationsWindow" 
    mc:Ignorable="d" 
    Title="{a:Text FavoriteLocationsWindow.Title}" 
    Height="400" 
    Width="800" 
    MinHeight="400" MinWidth="800" 
    Style="{StaticResource {x:Type Window}}" 
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <c:BindingProxy x:Key="DataContextProxy" Data="{Binding}" />
            <Style x:Key="ImageButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                <Style.Setters>
                    <Setter Property="Margin" Value="2" />
                    <Setter Property="Width" Value="22" />
                    <Setter Property="Height" Value="22" />
                </Style.Setters>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="4" SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{a:Text FavoriteLocationsWindow.Empty}" IsEnabled="False" Visibility="{Binding IsEmptyList, Converter={a:BoolToVisibilityConverter}}" />

        <DockPanel Grid.Row="0" VerticalAlignment="Stretch">

            <c:BindableListView 
                x:Name="List"
                MouseDoubleClickCommand="{Binding EditCommand}"
                VerticalAlignment="Stretch" 
                ItemsSource="{Binding FavoriteLocations}" 
                Visibility="{Binding IsEmptyList, Converter={a:InverseBoolToVisibilityConverter}}"
                SelectionMode="Single"                
                VerticalContentAlignment="Top"
                IsSynchronizedWithCurrentItem="True"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                ScrollViewer.CanContentScroll="False">
                <ListView.Resources>
                    <Style TargetType="{x:Type c:BindableListView}" BasedOn="{StaticResource ListViewWithGridViewStyle}">
                        <Setter Property="ItemContainerStyle">
                            <Setter.Value>
                                <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemGridViewStyle}" />
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                        <GridView.Columns>
                            <GridViewColumn Header="{a:Text FavoriteLocationsWindow.Location.Name}" Width="200" DisplayMemberBinding="{Binding Path=Name, Mode=OneWay}" />
                            <GridViewColumn Header="{a:Text FavoriteLocationsWindow.Location.Latitude}" Width="130" DisplayMemberBinding="{Binding Path=Latitude, Converter={a:LatitudeConverter}, Mode=OneWay}" />
                            <GridViewColumn Header="{a:Text FavoriteLocationsWindow.Location.Longitude}" Width="130" DisplayMemberBinding="{Binding Path=Longitude, Converter={a:LongitudeConverter}, Mode=OneWay}" />
                            <GridViewColumn Header="{a:Text FavoriteLocationsWindow.Location.UtcOffset}" Width="100" DisplayMemberBinding="{Binding Path=UtcOffset, Converter={a:TimeZoneConverter}, Mode=OneWay}" />
                            <GridViewColumn Header="{a:Text FavoriteLocationsWindow.Location.Landscape}" Width="180" DisplayMemberBinding="{Binding Path=Landscape, Mode=OneWay, Converter={a:EmptyStringToPlaceholderConverter}, ConverterParameter={a:Text Landscape.Default}}" />
                        </GridView.Columns>
                    </GridView>
                </ListView.View>
            </c:BindableListView>
        </DockPanel>

        <Separator Grid.Row="1" Margin="0 4 0 4" />

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Style="{StaticResource ImageButton}" Command="{Binding AddCommand}">
                <c:PathIcon Width="10" Height="10" Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" />
            </Button>
            <Button Style="{StaticResource ImageButton}" IsEnabled="{Binding ElementName=List, Path=SelectedValue, Converter={a:NotNullToBoolConverter}}" Command="{Binding EditCommand}" CommandParameter="{Binding ElementName=List, Path=SelectedValue}">
                <c:PathIcon Width="10" Height="10" Data="F1 M 26.9166,22.1667L 37.9999,33.25L 49.0832,22.1668L 53.8332,26.9168L 42.7499,38L 53.8332,49.0834L 49.0833,53.8334L 37.9999,42.75L 26.9166,53.8334L 22.1666,49.0833L 33.25,38L 22.1667,26.9167L 26.9166,22.1667 Z" />
            </Button>
            <Button  Style="{StaticResource ImageButton}" IsEnabled="{Binding ElementName=List, Path=SelectedValue, Converter={a:NotNullToBoolConverter}}" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=List, Path=SelectedValue}">
                <c:PathIcon Width="10" Height="10" Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z" />
            </Button>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button  MinWidth="80" Width="Auto" Margin="4 0 0 0" Padding="8 2" Command="{Binding CloseCommand}" Content="{a:Text Close}" />
        </StackPanel>

    </Grid>
</Window>
