<Window x:Class="Astrarium.Views.ViewAngleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:Astrarium.Types.Controls;assembly=Astrarium.Types"
        xmlns:s="clr-namespace:Astrarium.Types.Themes;assembly=Astrarium.Types"
        xmlns:t="clr-namespace:Astrarium.Types;assembly=Astrarium.Types"
        xmlns:local="clr-namespace:Astrarium.Views"
        mc:Ignorable="d"
        Title="{t:Text ViewAngleWindow.Title}" 
        Height="200" Width="300"
        ResizeMode="NoResize"
        Style="{StaticResource {x:Type Window}}"
        s:WindowProperties.MinButtonVisible="Collapsed"
        s:WindowProperties.MaxButtonVisible="Collapsed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" VerticalAlignment="Center">

            <TextBlock Margin="20 4" IsEnabled="False" Text="{t:Text ViewAngleWindow.Description}" HorizontalAlignment="Center" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <c:NumericUpDown Value="{Binding Degrees}" Minimum="0" Maximum="360" DecimalPlaces="0" Margin="4" HorizontalAlignment="Center" MinWidth="50" />
                <TextBlock Text="°" />
                <c:NumericUpDown Value="{Binding Minutes}" Minimum="0" Maximum="59" DecimalPlaces="0" Margin="4" HorizontalAlignment="Center" MinWidth="50" />
                <TextBlock Text="′" />
                <c:NumericUpDown Value="{Binding Seconds}" Minimum="0" Maximum="59" DecimalPlaces="0" Margin="4" HorizontalAlignment="Center" MinWidth="50" />
                <TextBlock Text="″" />
            </StackPanel>

            <Grid Margin="20 20 20 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Slider Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Minimum="{Binding MinFov, Converter={s:LogScaleConverter}}" Maximum="{Binding MaxFov, Converter={s:LogScaleConverter}}" Value="{Binding ViewAngle, Converter={s:LogScaleConverter}}" />

                <TextBlock IsEnabled="False" FontSize="10" Grid.Row="1" Grid.Column="0" Text="{Binding MinFov, Converter={s:FormatterConverter}, ConverterParameter={x:Type t:Formatters+AngleFormatter}, FallbackValue='min'}" HorizontalAlignment="Left" />
                <TextBlock IsEnabled="False" FontSize="10" Grid.Row="1" Grid.Column="2" Text="{Binding MaxFov, Converter={s:FormatterConverter}, ConverterParameter={x:Type t:Formatters+AngleFormatter}, FallbackValue='max'}" HorizontalAlignment="Right" />
               
            </Grid>

        </StackPanel>
        
        <Separator Grid.Row="1" />

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="{t:Text Select}" MinWidth="80"  Margin="6 0 0 0" Command="{Binding SelectCommand}" />
            <Button Content="{t:Text Cancel}" MinWidth="80" Margin="6" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</Window>
