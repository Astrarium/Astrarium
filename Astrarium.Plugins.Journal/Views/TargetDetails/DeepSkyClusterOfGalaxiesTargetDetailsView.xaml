<UserControl x:Class="Astrarium.Plugins.Journal.Views.DeepSkyClusterOfGalaxiesTargetDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:Astrarium.Plugins.Journal.Views" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <v:DeepSkyTargetDetailsView DataContext="{Binding}" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="Title" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Magnitude of 10th member: " Grid.Column="0" Grid.Row="0" Margin="2" VerticalAlignment="Center" />
            <TextBox Text="{Binding Mag10}" Grid.Column="1"  Grid.Row="0" Margin="2" IsReadOnly="True" />
        </Grid>
    </StackPanel>
</UserControl>
