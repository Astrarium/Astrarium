<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:t="clr-namespace:Astrarium.Types.Themes;assembly=Astrarium.Types"
                    xmlns:c="clr-namespace:Astrarium.Types.Controls;assembly=Astrarium.Types"
                    xmlns:q="clr-namespace:Astrarium.Plugins.ObservationsLog.Controls"
                    mc:Ignorable="d">

    <Style TargetType="{x:Type q:MultilineEdit}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type q:MultilineEdit}">
                    <Grid Margin="0 0 0 8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Label FontWeight="Bold" Foreground="{StaticResource BrushControlDarkBackground}" Grid.Row="0" Content="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Title}" />

                        <Button 
                            Focusable="False"
                            Name="PART_ApplyButton"
                            Background="{StaticResource BrushControlDarkBackground}"
                            BorderBrush="{StaticResource BrushControlLightBackground}"
                            BorderThickness="1"
                            Visibility="{TemplateBinding IsEditMode, Converter={t:BoolToVisibilityConverter}}"
                            Grid.Row="0" Grid.Column="1" Margin="1" Padding="8 0" Width="Auto" Height="24" HorizontalAlignment="Right" VerticalAlignment="Bottom" >
                            <Run FontSize="10">SAVE</Run>
                        </Button>

                        <TextBox                             
                            Name="PART_Editor" 
                            Padding="2"
                            Grid.Row="1" 
                            Grid.ColumnSpan="2"
                            BorderThickness="1"
                            IsReadOnly="{TemplateBinding IsEditMode, Converter={t:InverseBoolConverter}}"
                            Foreground="{StaticResource BrushForeground}"
                            Height="Auto"
                            AcceptsReturn="True"
                            VerticalAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            TextWrapping="Wrap"
                            Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Text}">
                            <TextBox.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Edit" />
                                </ContextMenu>
                            </TextBox.ContextMenu>
                        </TextBox>

                        <TextBlock
                            Name="PART_Placeholder"
                            Padding="4 2"
                            Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Placeholder}"
                            Visibility="{Binding Path=Text, ElementName=PART_Editor, Converter={q:EmptyToVisibilityConverter}}"
                            Foreground="{StaticResource BrushControlLightBackground}"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            Cursor="Hand"
                            Grid.Row="1" Grid.ColumnSpan="2" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditMode}" Value="False">
                            <Setter TargetName="PART_Editor" Property="BorderBrush" Value="Transparent" />
                            <Setter TargetName="PART_Editor" Property="IsReadOnly" Value="True" />
                            <Setter TargetName="PART_Editor" Property="ToolTip" Value="Double click to edit" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditMode}" Value="True">
                            <Setter TargetName="PART_Editor" Property="BorderBrush" Value="{StaticResource BrushControlBackground}" />
                            <Setter TargetName="PART_Editor" Property="IsReadOnly" Value="False" />
                            <Setter TargetName="PART_Editor" Property="ToolTip" Value="Shift+Enter to apply" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type q:TimeEdit}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type q:TimeEdit}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Name="PART_Placeholder"
                            Padding="4 2"
                            Text="--:--"
                            Visibility="Collapsed"
                            Foreground="{StaticResource BrushControlLightBackground}"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            Cursor="Hand" />

                        <TextBox Name="PART_Input" Visibility="Visible" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" BorderThickness="0" Width="44" Padding="3" />
                    </Grid>
                    <!--<ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" SourceName="PART_Input" Value="True" />
                                <Condition Property="IsEnabled" SourceName="PART_Input" Value="True" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter Property="BorderThickness" TargetName="PART_Input" Value="1" />
                                <Setter Property="Padding" TargetName="PART_Input" Value="2" />
                            </MultiTrigger.Setters>
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsKeyboardFocused" SourceName="PART_Input" Value="True" />
                                <Condition Property="IsEnabled" SourceName="PART_Input" Value="True" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter Property="BorderThickness" TargetName="PART_Input" Value="1" />
                                <Setter Property="Padding" TargetName="PART_Input" Value="2" />
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>-->
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>