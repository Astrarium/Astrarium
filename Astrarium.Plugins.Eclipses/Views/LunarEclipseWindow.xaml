<a:MouseEventsInterceptableWindow 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Astrarium.Plugins.Eclipses" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:a="Astrarium" 
    xmlns:q="clr-namespace:Astrarium.Plugins.Eclipses.Controls" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    x:Class="Astrarium.Plugins.Eclipses.LunarEclipseWindow" 
    mc:Ignorable="d" 
    Title="{a:Text Eclipses.Lunar}"
    Height="768" 
    Width="1024" 
    WindowState="Maximized"
    MinHeight="600" MinWidth="800" 
    Style="{StaticResource {x:Type Window}}"     
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="./Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="0">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="{StaticResource ImagePrevSaros}" Style="{StaticResource ImageButton}" Command="{Binding PrevSarosCommand}" IsEnabled="{Binding PrevSarosEnabled}" ToolTip="{a:Text EclipseView.PrevSaros}" />
                <Button Content="{StaticResource ImagePrevEclipse}" Style="{StaticResource ImageButton}" Command="{Binding PrevEclipseCommand}" ToolTip="{a:Text EclipseView.Prev}" />
                <Button MinWidth="200" VerticalAlignment="Center" Command="{Binding ChangeDateCommand}" Style="{StaticResource DateButton}" ToolTip="{a:Text EclipseView.ChangeDate}">
                    <Button.Content>
                        <TextBlock FontSize="32" VerticalAlignment="Center"  Text="{Binding EclipseDate, FallbackValue='01 Jan 2000'}" />
                    </Button.Content>
                </Button>
                <Button Content="{StaticResource ImageNextEclipse}" Style="{StaticResource ImageButton}" Command="{Binding NextEclipseCommand}" ToolTip="{a:Text EclipseView.Next}" />
                <Button Content="{StaticResource ImageNextSaros}" Style="{StaticResource ImageButton}" Command="{Binding NextSarosCommand}" IsEnabled="{Binding NextSarosEnabled}" ToolTip="{a:Text EclipseView.NextSaros}" />
            </StackPanel>
            <TextBlock Height="Auto" Padding="0 0 0 10" HorizontalAlignment="Center" Text="{Binding EclipseDescription, FallbackValue='Eclipse description'}"></TextBlock>
        </StackPanel>

        <Grid Height="6" Grid.Row="1">
            <ProgressBar Height="6" Background="Transparent" IsIndeterminate="True" Visibility="{Binding IsCalculating, Converter={a:BoolToVisibilityConverter}}" />
        </Grid>

        <TabControl Grid.Row="2" Padding="0" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="{a:Text EclipseView.Tab.Map}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="148*"/>
                        <ColumnDefinition Width="19*"/>
                        <ColumnDefinition Width="213*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="460"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="1,-1,8,0" Grid.ColumnSpan="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="115*"/>
                            <ColumnDefinition Width="268*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <a:MapControl
                            x:Name="Map"
                            Grid.Row="0"
                            BackColor="{Binding MapThumbnailBackColor}"                       
                            TileServer="{Binding TileServer, Mode=TwoWay}" 
                            OverlayOpacity="{Binding OverlayOpacity, Mode=TwoWay}"
                            OverlayTileServer="{Binding OverlayTileServer, Mode=TwoWay}"
                            CacheFolder="{Binding CacheFolder, Mode=OneTime}"
                            IsUpdating="{Binding IsCalculating}"
                            Markers="{Binding Markers}"
                            Tracks="{Binding Tracks}"
                            Center="{Binding MapCenter, Mode=TwoWay}"
                            ZoomLevel="{Binding MapZoomLevel, Mode=TwoWay}"
                            Polygons="{Binding Polygons}"
                            ThumbnailBackColor="{Binding MapThumbnailBackColor}"
                            ThumbnailForeColor="{Binding MapThumbnailForeColor}"
                            IsMouseOverMap = "{Binding IsMouseOverMap, Mode=OneWayToSource}"
                            TileImageAttributes="{Binding TileImageAttributes}"
                            OnDoubleClick="{Binding LockOnCurrentPositionCommand}"
                            OnRightClick="{Binding RightClickOnMapCommand}"
                            Mouse="{Binding MapMouse, Mode=OneWayToSource}" Grid.ColumnSpan="2" Margin="0,0,0.4,25.4" Grid.RowSpan="2">
                            <a:MapControl.ContextMenu>
                                <ContextMenu Opened="ContextMenu_Opened">
                                    <MenuItem Style="{StaticResource MenuItemHeader}" Header="{Binding MapMouseString}" IsEnabled="False" />
                                    <MenuItem Header="{a:Text EclipseView.ContextMenu.Lock}" Command="{Binding LockOnCurrentPositionCommand}" />
                                    <MenuItem Header="{a:Text EclipseView.ContextMenu.AddToCitiesList}" Command="{Binding AddCurrentPositionToCitiesListCommand}" />
                                    <MenuItem Style="{StaticResource MenuItemHeader}" Visibility="{Binding NearestLocation, Converter={a:NotNullToVisibilityConverter}}" Header="{Binding NearestLocation.LocationName}" />
                                    <MenuItem Header="{a:Text EclipseView.ContextMenu.Lock}" Visibility="{Binding NearestLocation, Converter={a:NotNullToVisibilityConverter}}" Command="{Binding LockOnNearestLocationCommand}" />
                                    <MenuItem Header="{a:Text EclipseView.ContextMenu.AddToCitiesList}" Command="{Binding AddNearestLocationToCitiesListCommand}" Visibility="{Binding NearestLocation, Converter={a:NotNullToVisibilityConverter}}" />
                                </ContextMenu>
                            </a:MapControl.ContextMenu>
                        </a:MapControl>

                        <Grid Height="26" Background="{StaticResource BrushControlDarkBackground}" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" MinWidth="200" MaxWidth="400" />
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{a:Text LocationWindow.Map}" Margin="2 0 4 0" />
                                <ComboBox Padding="4 -1" Margin="2 0" Width="140" Height="18" ItemsSource="{Binding TileServers, Mode=OneTime}" DisplayMemberPath="Name" SelectedItem="{Binding TileServer}" />
                                <TextBlock Text="{a:Text LocationWindow.Overlay}" Margin="4 0 4 0" />

                                <a:NullItemSelectorAdapter NullItem="{a:Text LocationWindow.Overlay.None}" ItemsSource="{Binding OverlayTileServers, Mode=OneTime}">
                                    <ComboBox Padding="4 -1" Margin="2 0" Width="100" Height="18" SelectedItem="{Binding OverlayTileServer}" />
                                </a:NullItemSelectorAdapter>

                                <ComboBox Padding="4 -1" Margin="2 0" Width="60" Height="18" ItemsSource="{Binding OverlayOpacities, Mode=OneTime}" SelectedItem="{Binding OverlayOpacity}" Visibility="{Binding OverlayTileServer, Converter={a:NotNullToVisibilityConverter}}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding StringFormat='P0'}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>

                            </StackPanel>

                            <Slider Padding="16 0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Column="1" Orientation="Horizontal" 
                                Minimum="{Binding ElementName=Map, Path=MinZoomLevel, Mode=OneWay}"
                                Maximum="{Binding ElementName=Map, Path=MaxZoomLevel, Mode=OneWay}"
                                Value="{Binding ElementName=Map, Path=ZoomLevel, Mode=TwoWay}" />
                            <a:HtmlTextBlock Grid.Column="3" Foreground="{StaticResource BrushControlLightBackground}" VerticalAlignment="Center" HorizontalAlignment="Right" Padding="2 0 4 0" FontSize="12" FormattedText="{Binding TileServer.AttributionText, FallbackValue='AttributionText'}" />
                        </Grid>
                        
                    </Grid>
                    <GridSplitter 
                        Grid.Column="3" 
                        ShowsPreview="False"   
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Stretch" Margin="2,0" Width="6" />

                    <!-- Local circumstances table title -->

                    <ScrollViewer Grid.Column="4"
                        x:Name="RightPanel"
                        SizeChanged="RightPanel_SizeChanged"
                        ScrollChanged="RightPanel_ScrollChanged"
                        HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="RightPanelStack" Orientation="Vertical">

                            <TextBlock Padding="6 2" FontSize="18" Text="{a:Text EclipseView.LocalCircumstances}" />

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock VerticalAlignment="Center" Padding="6 4" Text="{Binding ObserverLocationName, FallbackValue='Observer location name'}" />
                                <ToggleButton Grid.Column="1" IsChecked="{Binding IsMapLocked}" Visibility="{Binding IsMapLocked, Converter={a:BoolToVisibilityConverter}}" Margin="4 0 4 0" Height="24" VerticalContentAlignment="Center" HorizontalAlignment="Right">
                                    <ToggleButton.Content>
                                        <TextBlock Padding="4" VerticalAlignment="Center" Text="{a:Text EclipseView.Unlock}" />
                                    </ToggleButton.Content>
                                </ToggleButton>
                            </Grid>

                            <TextBlock Padding="6 2" TextTrimming="CharacterEllipsis" Width="{Binding ElementName=RightPanel, Path=ActualWidth}" Text="{Binding LocalVisibilityDescription, FallbackValue='Local visibility description'}" HorizontalAlignment="Left" />
                            
                            <!-- Local details expander -->
                            <Expander Margin="0 6 0 4" Header="{a:Text EclipseView.LocalDetails}" IsExpanded="True">
                                <StackPanel Orientation="Vertical">
                                    <!-- Local contacts table -->
                                    <DataGrid 
                                        VerticalScrollBarVisibility="Auto"
                                        EnableRowVirtualization="True"
                                        EnableColumnVirtualization="True"
                                        VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                                        VirtualizingPanel.VirtualizationMode="Recycling"
                                        VirtualizingPanel.IsVirtualizing="True"
                                        VerticalAlignment="Stretch"
                                        AutoGenerateColumns="False" 
                                        IsReadOnly="True"
                                        CanUserReorderColumns="False"
                                        CanUserSortColumns="False"
                                        GridLinesVisibility="All"
                                        BorderThickness="1"
                                        Margin="0 2 0 2"
                                        Padding="2"
                                        RowHeaderWidth="0"
                                        ColumnHeaderHeight="32"
                                        RowHeight="24"
                                        ItemsSource="{Binding Path=LocalContactsTable, Mode=OneWay}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{a:Text EclipseView.LocalContacts.Instant}" Binding="{Binding Point}" />
                                            <DataGridTextColumn Header="{a:Text EclipseView.LocalContacts.Time}" FontFamily="Lucida Console" Binding="{Binding Time}"/>
                                            <DataGridTextColumn Header="{a:Text EclipseView.LocalContacts.Alt}" FontFamily="Lucida Console" Binding="{Binding Altitude}"/>
                                            <DataGridTextColumn Header="P" FontFamily="Lucida Console" Binding="{Binding PAngle}"/>
                                            <DataGridTextColumn Header="Z" FontFamily="Lucida Console" Binding="{Binding ZAngle}"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </Expander>

                            <!-- Eclipse chart -->
                            <Expander Header="{a:Text EclipseView.LocalView}" IsExpanded="True" >
                                <StackPanel Orientation="Vertical">
                                    <!-- Chart header -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <!-- Eclipse contacts toggler -->
                                        <StackPanel Margin="2" Grid.Column="0" HorizontalAlignment="Left" SnapsToDevicePixels="True" Orientation="Horizontal">
                                            <RadioButton Content="P1" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.P1}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.P1}" />
                                            <RadioButton Content="U1" IsEnabled="{Binding LocalCircumstances.PartialBegin, Converter={a:NotNullToBoolConverter}}" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.U1}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.U1}" />
                                            <RadioButton Content="U2" IsEnabled="{Binding LocalCircumstances.TotalBegin, Converter={a:NotNullToBoolConverter}}" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.U2}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.U2}" />
                                            <RadioButton Content="Max" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.Max}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.Max}" />
                                            <RadioButton Content="U3" IsEnabled="{Binding LocalCircumstances.TotalEnd, Converter={a:NotNullToBoolConverter}}" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.U3}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.U3}" />
                                            <RadioButton Content="U4" IsEnabled="{Binding LocalCircumstances.PartialEnd, Converter={a:NotNullToBoolConverter}}" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.U4}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.U4}" />
                                            <RadioButton Content="P4" IsChecked="{Binding ElementName=Chart, Path=Contact, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartContact.P4}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text LunarEclipseView.LocalCircumstances.P4}" />
                                        </StackPanel>
                                        <!-- Chart orientation toggler -->
                                        <StackPanel Margin="2" Grid.Column="2" HorizontalAlignment="Right" SnapsToDevicePixels="True" Orientation="Horizontal">
                                            <RadioButton Content="Z" IsChecked="{Binding ElementName=Chart, Path=Orientation, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartOrientation.Zenithal}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text EclipseView.LocalView.Zenithal}" />
                                            <RadioButton Content="N" IsChecked="{Binding ElementName=Chart, Path=Orientation, Converter={a:EqualityConverter}, ConverterParameter={x:Static q:LunarEclipseChartOrientation.Equatorial}}" Style="{StaticResource ResourceKey=RadioTab}" ToolTip="{a:Text EclipseView.LocalView.Equatorial}" />
                                        </StackPanel>
                                    </Grid>

                                    <!-- Local circumstances chart -->
                                    <q:LunarEclipseChartControl 
                                        x:Name="Chart"
                                        IsEnabled="{Binding IsVisibleFromCurrentPlace, Mode=OneWay}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        MinHeight="300"
                                        DarkMode="{Binding IsDarkMode, Mode=OneWay}"
                                        ZoomLevel="{Binding ChartZoomLevel}"
                                        Circumstances="{Binding LocalCircumstances}" />

                                    <!-- Chart footer -->
                                    <DockPanel Margin="0" HorizontalAlignment="Stretch">
                                        <!-- Chart options togglers -->
                                        <ToggleButton IsChecked="{Binding ElementName=Chart, Path=ShowLabels}" ToolTip="{a:Text EclipseView.LocalView.ShowLabels}" Width="24" Height="24" Content="{StaticResource ImageChartLabels}" />
                                        <ToggleButton IsChecked="{Binding ElementName=Chart, Path=ShowContactCircles}" ToolTip="{a:Text EclipseView.LocalView.ShowContactCircles}" Width="24" Height="24" Content="{StaticResource ImageChartCircles}" />
                                        <!-- Chart zoom buttons -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Command="{Binding ChartZoomInCommand}" Content="{StaticResource ImageZoomIn}" ToolTip="{a:Text EclipseView.LocalView.ZoomIn}" Height="22" Width="22" Margin="2" Padding="-3 -1 0 0" />
                                            <Button Command="{Binding ChartZoomOutCommand}" Content="{StaticResource ImageZoomOut}" ToolTip="{a:Text EclipseView.LocalView.ZoomOut}" Height="22" Width="22" Margin="2" Padding="-3 -1 0 0" />
                                        </StackPanel>
                                    </DockPanel>
                                </StackPanel>
                            </Expander>

                            <StackPanel x:Name="RightPanelFooter" Orientation="Vertical">
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </TabItem>
            <TabItem Header="{a:Text EclipseView.Tab.GeneralInfo}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                    <Grid ScrollViewer.CanContentScroll="True" IsEnabled="{Binding IsCalculating, Converter={a:InverseBoolConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- General details table title -->
                        <TextBlock Grid.Row="0" FontSize="18" Padding="8 8 0 0" Text="{a:Text EclipseView.EclipseDetails}" />

                        <!-- General details table -->
                        <DataGrid Grid.Row="1"                            
                            VerticalScrollBarVisibility="Auto"
                            VerticalAlignment="Stretch"
                            AutoGenerateColumns="False" 
                            IsReadOnly="True"
                            CanUserReorderColumns="False"
                            CanUserSortColumns="False"
                            HeadersVisibility="None"
                            GridLinesVisibility="All"
                            BorderThickness="1"
                            Margin="8"
                            Padding="2"
                            RowHeaderWidth="0"
                            ColumnHeaderHeight="32"
                            RowHeight="24"
                            ItemsSource="{Binding EclipseGeneralDetails}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                                <DataGridTextColumn Header="Value" Binding="{Binding Value}"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Contacts table title -->
                        <TextBlock Grid.Row="2" FontSize="18" Padding="8 8 0 0" Text="{a:Text LunarEclipseView.EclipseContacts}" />

                        <!-- Contacts table -->
                        <DataGrid Grid.Row="3"
                            VerticalScrollBarVisibility="Auto"
                            VerticalAlignment="Stretch"
                            CanUserReorderColumns="False"
                            CanUserSortColumns="False"
                            AutoGenerateColumns="False" 
                            IsReadOnly="True"
                            GridLinesVisibility="All"
                            BorderThickness="1"
                            Margin="8"
                            Padding="2"
                            RowHeaderWidth="0"
                            ColumnHeaderHeight="32"
                            RowHeight="24"
                            ItemsSource="{Binding EclipseContacts}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{a:Text LunarEclipseView.EclipseContacts.Point}" Binding="{Binding Point}"/>
                                <DataGridTextColumn Header="{a:Text LunarEclipseView.EclipseContacts.Time}" Binding="{Binding Time}"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Besselian elements table title -->
                        <TextBlock Grid.Row="4" FontSize="18" Padding="8 8 0 0" Text="{a:Text EclipseView.BesselianElements}" />

                        <!-- Besselian elements table header (displaying t0 timings) -->
                        <TextBlock Grid.Row="5" Padding="8 8 0 0" Text="{Binding BesselianElementsTableHeader}" />

                        <!-- Besselian elements table -->
                        <DataGrid Grid.Row="6"
                            VerticalScrollBarVisibility="Auto"
                            VerticalAlignment="Stretch"
                            CanUserReorderColumns="False"
                            CanUserSortColumns="False"
                            AutoGenerateColumns="False" 
                            IsReadOnly="True"
                            GridLinesVisibility="All"
                            BorderThickness="1"
                            Margin="8 -8 8 0"
                            Padding="2"
                            RowHeaderWidth="0"
                            ColumnHeaderHeight="32"
                            RowHeight="24"
                            ItemsSource="{Binding BesselianElementsTable}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="n" Binding="{Binding Index}"/>
                                <DataGridTextColumn Header="x" Binding="{Binding X}"/>
                                <DataGridTextColumn Header="y" Binding="{Binding Y}"/>
                                <DataGridTextColumn Header="F1" Binding="{Binding F1}"/>
                                <DataGridTextColumn Header="F2" Binding="{Binding F2}"/>
                                <DataGridTextColumn Header="F3" Binding="{Binding F3}"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Besselian elements table footer (displaying tanF1 and tanF2)-->
                        <TextBlock Grid.Row="7" Padding="8" Text="{Binding BesselianElementsTableFooter}" />

                    </Grid>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="{a:Text EclipseView.Tab.SarosSeries}">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" FontSize="18" Padding="8 8 0 0" Text="{Binding SarosSeriesTableTitle, FallbackValue='List of eclipses of saros series 123'}" Grid.ColumnSpan="2" />
                    <Button Margin="0 8 8 0" Padding="8 0" Grid.Column="2" HorizontalAlignment="Right" Content="{a:Text EclipseView.Export}" Command="{Binding ExportSarosSeriesTableCommand}" />

                    <DataGrid Grid.Row="1"
                        IsEnabled="{Binding IsCalculating, Converter={a:InverseBoolConverter}}"
                        VerticalScrollBarVisibility="Auto"
                        VerticalAlignment="Stretch"
                        AutoGenerateColumns="False" 
                        CanUserReorderColumns="False"
                        CanUserSortColumns="False"
                        IsReadOnly="True"
                        GridLinesVisibility="All"
                        BorderThickness="1"
                        Margin="8"
                        Padding="2"
                        RowHeaderWidth="0"
                        ColumnHeaderHeight="32"
                        RowHeight="24"
                        ItemsSource="{Binding SarosSeriesTable}" Grid.ColumnSpan="2">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{a:Text EclipseView.SarosTable.Member}" Binding="{Binding Member}"/>
                            <DataGridTemplateColumn Header="{a:Text EclipseView.SarosTable.Date}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontFamily="Lucida Console" FontSize="12">
                                        <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:LunarEclipseWindow}, Mode=FindAncestor}, Path=DataContext.SarosSeriesTableSetDateCommand}" CommandParameter="{Binding MeeusLunationNumber}"><Run Text="{Binding Date}" /></Hyperlink>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{a:Text EclipseView.SarosTable.Type}" Binding="{Binding Type}"/>
                            <DataGridTextColumn Header="{a:Text EclipseView.SarosTable.Gamma}" Binding="{Binding Gamma}"/>
                            <DataGridTextColumn Header="{a:Text EclipseView.SarosTable.Magnitude}" Binding="{Binding Magnitude}"/>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Style.Setters>
                                            <Setter Property="Content" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:LunarEclipseWindow}, Mode=FindAncestor}, Path=DataContext.SettingsLocationName}" />
                                        </Style.Setters>
                                    </Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontSize="12" Text="{Binding LocalVisibility}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

            </TabItem>
            <TabItem Header="{a:Text EclipseView.Tab.LocalCircumstances}">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="18" Padding="8 8 0 0" Text="{a:Text EclipseView.LocalCircumstances}" />
                        <Button Margin="0 8 8 0" Padding="8 0" Grid.Column="1" HorizontalAlignment="Right" Content="{a:Text EclipseView.Clear}" Command="{Binding ClearLocationsTableCommand}" Visibility="{Binding IsCitiesListTableNotEmpty, Converter={a:BoolToVisibilityConverter}}" />
                        <Button Margin="0 8 8 0" Padding="8 0" Grid.Column="2" HorizontalAlignment="Right" Content="{a:Text EclipseView.Export}" Command="{Binding ExportLocationsTableCommand}" Visibility="{Binding IsCitiesListTableNotEmpty, Converter={a:BoolToVisibilityConverter}}" />
                    </Grid>
                    
                    <!-- Import options view, displayed if no locations loaded -->
                    <Grid Grid.Row="1" Margin="0 10" Visibility="{Binding IsCitiesListTableNotEmpty, Converter={a:InverseBoolToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" Grid.Column="0" Margin="8 4" Padding="8 0" Content="{a:Text EclipseView.Import}" Width="Auto" HorizontalAlignment="Stretch" Command="{Binding LoadLocationsFromFileCommand}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Padding="0 " VerticalAlignment="Center">                                
                            <Run Text="{a:Text EclipseView.Import.Text}" /> <Hyperlink Command="{Binding ShowLocationsFileFormatCommand}"><Run Text="{a:Text EclipseView.Import.Format}" /></Hyperlink>.
                        </TextBlock>
                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"  Padding="8 4" VerticalAlignment="Center" Text="{a:Text EclipseView.AddLocationFromMap}" />
                    </Grid>
                    
                    <!-- Locations list table -->
                    <DataGrid Grid.Row="2"
                        VerticalScrollBarVisibility="Auto"
                        VerticalAlignment="Stretch"
                        AutoGenerateColumns="False" 
                        CanUserReorderColumns="False"
                        CanUserSortColumns="True"
                        EnableRowVirtualization="True"
                        EnableColumnVirtualization="True"
                        VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                        VirtualizingPanel.VirtualizationMode="Recycling"
                        VirtualizingPanel.IsVirtualizing="True"
                        IsReadOnly="True"
                        Visibility="{Binding IsCitiesListTableNotEmpty, Converter={a:BoolToVisibilityConverter}}"
                        GridLinesVisibility="All"
                        BorderThickness="1"
                        Margin="8"
                        Padding="2"
                        RowHeaderWidth="0"
                        ColumnHeaderHeight="32"
                        RowHeight="24"
                        ItemsSource="{Binding CitiesListTable}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="{a:Text EclipseView.LocalCircumstancesTable.Location}" CanUserSort="True" SortMemberPath="LocationName">
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text EclipseView.LocalCircumstancesTable.Location.Tooltip}" />
                                    </Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Hyperlink Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:LunarEclipseWindow}, Mode=FindAncestor}, Path=DataContext.CitiesListTableGoToCoordinatesCommand}" CommandParameter="{Binding Coordinates, Mode=OneWay}"><Run Text="{Binding LocationName, Mode=OneWay}" ToolTip="{a:Text EclipseView.LocalCircumstancesTable.Location.LinkTooltip}" /></Hyperlink>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{a:Text EclipseView.LocalCircumstancesTable.Coordinates}" Binding="{Binding CoordinatesString}" CanUserSort="False">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text EclipseView.LocalCircumstancesTable.Coordinates.Tooltip}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{a:Text EclipseView.LocalCircumstancesTable.TimeZone}" Binding="{Binding TimeZoneString}" SortMemberPath="TimeZone" CanUserSort="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text EclipseView.LocalCircumstancesTable.TimeZone.Tooltip}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="P1" Binding="{Binding P1String}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.P1}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="U1" Binding="{Binding U1String}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.U1}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="U2" Binding="{Binding U2String}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.U2}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Max" Binding="{Binding MaxString}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.Max}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="U3" Binding="{Binding U3String}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.U3}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="U4" Binding="{Binding U4String}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.U4}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="P4" Binding="{Binding P4String}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text LunarEclipseView.LocalCircumstances.P4}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="{a:Text EclipseView.LocalCircumstancesTable.Visibility}" Binding="{Binding Visibility}" CanUserSort="False">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="ToolTip" Value="{a:Text EclipseView.LocalCircumstancesTable.Visibility.Tooltip}" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</a:MouseEventsInterceptableWindow>