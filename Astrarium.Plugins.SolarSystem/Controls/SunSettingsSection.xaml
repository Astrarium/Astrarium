<c:SettingsSection 
    x:Class="Astrarium.Plugins.SolarSystem.Controls.SunSettingsSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:t="clr-namespace:Astrarium.Types;assembly=Astrarium.Types"
    xmlns:c="clr-namespace:Astrarium.Types.Controls;assembly=Astrarium.Types"
    xmlns:s="clr-namespace:Astrarium.Types.Themes;assembly=Astrarium.Types"
    Title="{t:Text Section.Sun}"
    Icon="{StaticResource IconSun}"
    mc:Ignorable="d"
    d:DesignHeight="450" 
    d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Margin="6">
        <CheckBox Content="{t:Text Settings.Sun}" IsChecked="{Binding Settings.Sun}" Margin="2" />
        <CheckBox Content="{t:Text Settings.SunLabel}" IsChecked="{Binding Settings.SunLabel}" IsEnabled="{Binding Settings.Sun}" Margin="2" />
        <CheckBox Content="{t:Text Settings.SunTexture}" IsChecked="{Binding Settings.SunTexture}" IsEnabled="{Binding Settings.Sun}" Margin="2" />
        <CheckBox Content="{t:Text Settings.SunFeatures}" IsChecked="{Binding Settings.SunFeatures}" IsEnabled="{Binding Settings.Sun}" Margin="2" />
        <CheckBox Content="{t:Text Settings.SunEquator}" IsChecked="{Binding Settings.SunEquator}" IsEnabled="{Binding Settings.Sun}" Margin="2" />
    </StackPanel>
</c:SettingsSection>
